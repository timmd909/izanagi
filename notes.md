install git ansible vim

disable cows in ansible

double check git config defaults
(had to set my name and email in git
with `git config --global --edit`)

update git default push so i don't have to do
    git push --set-upstream origin 2026-devuan-trial
or similar again

the standard shell scripts (like log-to-last-merge, etc)
should be added to the ansible scripts
